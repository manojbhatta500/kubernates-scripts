create nginx pod imparitavely  through kuebctl 

 	cmd : kubectl run nginix-pod --image=ngnix:latest 

	cmd : kubectl get pods
	cmd : kubectl explain pod 
	
	- yml for kubernates  apiversion kind metadata  specs
	cmd : kubectl create -f pod.yml
	cmd : kubectl delete pod nginx  
	cmd : kubectl describe pod podname 
	cmd : kubectl edit  pod nginix-pod
	cmd : kubectl exec -it podname --sh 


	cmd : kubectl run nginix --image=nginix --dry-run=client -o yaml > pod-n	ew.yaml
	
	
 



1.  What are Pods in Kubernetes.

2.  Imperative vs declarative way of interacting with cluster

3. How to create pods using Imperative commands

4. YAML tutorial for beginners

5. How to create and manage pods using YAML

day 7  21:09 min


Task 1

Create a pod using the imperative command and use nginx as the image
Task2

Create the YAML from the nginx pod created in task 1
Update the pod name in the YAML
Use that YAML to create a new pod with the name nginx-new.
Task3

Apply the below YAML and fix the errors, including all the commands that you run during the troubleshooting and the error message


apiVersion: v1
kind: Pod
metadata:
  labels:
    app: test
  name: redis
spec:
  containers:
  - image: rediss
    name: redis
    

:
